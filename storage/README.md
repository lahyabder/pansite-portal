# Storage

This directory contains uploaded files for the GED module.
Each document gets its own subdirectory: `/storage/uploads/{doc-id}/`

In production, this would be replaced by an object storage service (S3, GCS, etc.).
